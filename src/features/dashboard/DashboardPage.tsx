import { Card } from '../../components/ui/Card'

/**
 * TODO: Overview, conditional sections, links to each module (PROJECT_OUTLINE.md — Dashboard).
 */
export default function DashboardPage() {
  return (
    <div className="page dashboard-page">
      <Card title="Dashboard">
        <p>
          TODO: Summarize each learning area and link to routes (see{' '}
          <code>PROJECT_OUTLINE.md</code>).
        </p>
      </Card>
    </div>
  )
}
