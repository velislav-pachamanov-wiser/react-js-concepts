import { Card } from '../../components/ui/Card'

/**
 * TODO: React Hook Form + Zod; submit to JSONPlaceholder (PROJECT_OUTLINE.md — Todos).
 */
export default function TodoForm() {
  return (
    <Card title="New todo">
      <p>TODO: Build validated form (title, completed, etc.).</p>
    </Card>
  )
}
