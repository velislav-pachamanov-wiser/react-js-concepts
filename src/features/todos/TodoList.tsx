import { Card } from '../../components/ui/Card'

/**
 * TODO: List todos, optimistic updates or refetch after mutation (PROJECT_OUTLINE.md).
 */
export default function TodoList() {
  return (
    <Card title="Todos">
      <p>TODO: Render todos from API or cache.</p>
    </Card>
  )
}
