import { Card } from '../../components/ui/Card'

/**
 * TODO: useAppSelector + users slice; table or list UI (PROJECT_OUTLINE.md — Users).
 */
export default function UsersTable() {
  return (
    <Card title="Users">
      <p>TODO: Render users from Redux state.</p>
    </Card>
  )
}
